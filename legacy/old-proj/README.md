# old-proj

Esta pasta passou a ser considerada **legada**.

Uso recomendado:

- executar o projeto por `python3 -m pipeline.main`
- usar `configs/*.json` como ponto de partida para novas execucoes
- consultar `old-proj/` apenas para resultados antigos, configs historicas e referencia de migracao

Observacoes:

- o runtime ativo do projeto foi priorizado em `shared/` e `models/` na raiz do repositorio
- `simple_pipeline.py` e `main.py` dentro de `old-proj/` nao sao mais a entrada principal recomendada
